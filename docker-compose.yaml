version: '3'
services:
  app-backend:
    container_name: go-fiber-boilerplate
    build: .
    env_file:
      - .env
    ports:
      - 8080:8080
    environment:
      - HOST=${HOST}
      - PORT=${PORT}
      - GO_ENV=${GO_ENV}
    volumes:
      - app-backend:/app

volumes:
  app-backend:
